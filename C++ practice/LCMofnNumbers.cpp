#include<iostream>
using namespace std;
int main(){

int size;
cout<<"\n\n enter how many num2 to find LCM";
cin>>size;

int numbers[size];
for(int i=0;i<size;i++){
    cin>>
}
/*
// bbut below will traouble later if to fimd LCm of n numbers since
    // then cannot make dynamic if to modulo% each num
            // therfore dropping this idea now
//simple elimiting first if of same family
>> if(num3%num2==0 && num3%num1==0)
    cout<<"\n LCM is "<<num3;
    */



    return 0;
}